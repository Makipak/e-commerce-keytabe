VITE_API_URL=http://localhost:4000
# URL API dari sudut pandang dev server Vite sendiri (proxy /uploads di vite.config.ts).
# Saat demo ngrok, admin dan API jalan di mesin yang sama → pakai localhost.
VITE_API_INTERNAL_URL=http://localhost:4000
